package it.formarete.designpatterns.calculator.strategy;

public class ValidatingCalculatorImpl implements ValidatingCalculator {

	@Override
	public int isValid(Integer firstNum, Integer secondNum) {
				return 0;
	}

}
